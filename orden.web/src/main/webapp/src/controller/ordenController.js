define(['controller/_ordenController','delegate/ordenDelegate'], function() {
    App.Controller.OrdenController = App.Controller._OrdenController.extend({

    });
    return App.Controller.OrdenController;
}); 